import React from 'react';
import './NetSelector.scss';

const NetSelector: React.FunctionComponent = () => {
  return (
    <div className="netSelector-wrapper">
      Networks go here.
    </div>
  )
}

export default NetSelector;
